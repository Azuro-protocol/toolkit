query Bets(
  $first: Int,
  $skip: Int,
  $where: V3_Bet_filter!,
) {
  v3Bets(
    first: $first,
    skip: $skip,
    where: $where,
    orderBy: createdBlockNumber,
    orderDirection: desc,
    subgraphError: allow
  ) {
    ...Bet
  }
}
